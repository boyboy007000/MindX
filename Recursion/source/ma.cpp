#include "ma.h"

void NuocDi(int n, int y, int x){// nuoc di thu n, xuat phat tu hang y cot x
    for (int i = 0; i < 8; i++){ // 8 nuoc di co the co
        // chon thu huong di thu i (trong huong 8 di)
        int dmoi = d + dd[i], cmoi = c + dc[j];
        // neu huong di nay hop le 
    }
}